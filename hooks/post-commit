#!/bin/bash
location="schurtransform/version.txt"
version=$(cat "$location")
prefix=$(echo "$version" | grep -o '^([\d]+\.[\d]+\.)')
microversion=$(echo "$version" | grep -o '([\d]+)$')
microversion=$(( microversion + 1 ))
echo -n "$prefix$microversion" > $location
